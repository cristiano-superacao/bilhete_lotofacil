# Variáveis de Ambiente - LotoFácil Estratégica

# Database (Neon PostgreSQL)
DATABASE_URL=postgresql://username:password@ep-example.us-east-1.aws.neon.tech/dbname?sslmode=require

# Node Environment
NODE_ENV=development

# Netlify Functions
NETLIFY_FUNCTIONS_PORT=9999

# API Keys (exemplo)
CAIXA_API_KEY=your-api-key-here

# Firebase (se usar)
FIREBASE_API_KEY=your-firebase-key
FIREBASE_PROJECT_ID=your-project-id

# Instruções:
# 1. Renomeie este arquivo para .env
# 2. Configure as variáveis com seus valores reais
# 3. Nunca commite o arquivo .env (já está no .gitignore)
# 4. No Netlify: vá em Site Settings > Environment Variables e adicione essas variáveis